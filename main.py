from common.calculo import *
from common.usuario import *
import time

DORMIR=10
if __name__=='__main__':
"""     operacion=input("Introducir la operación S:Sumar, R:Restar,M:Multiplicar,D:Dividir")
    if operacion.upper() not in ("S","R","M","D"):
        print("La operación no es correcta")
    else:
        numero1=float(input("Introduzca el numero 1"))
        numero2=float(input("Introduzca el numero 2"))
        if(operacion.upper()=="S"):
            suma=sumar(numero1,numero2)
            mensaje("suma",suma)
        elif(operacion.upper()=="R"):
            diferencia=restar(numero1,numero2)
            mensaje("diferencia",diferencia)
        elif(operacion.upper()=="M"):
            multiplicacion=multiplicar(numero1,numero2)
            mensaje("multiplicacion",multiplicacion)
        elif(operacion.upper()=="D"):
            division=dividir(numero1,numero2)
            if division==None:
                print("error de division por 0")
            else:
                mensaje("division",division) """

"""     largo=float(input("introduce el largo"))
    ancho=float(input("introduce el ancho"))
    metros=metros_cuadrados(largo,ancho)
    print(f"los metros cuadrados son {metros:.2f} metros cuadrados") """

"""     time.sleep(DORMIR)
    msg()
    time.sleep(DORMIR)
    msg_medio()
    time.sleep(DORMIR)
    msg_final() """

"""     print("un programa para repetir un numero")
    numero=get_numero()
    repetir_numero(numero,10) """

"""     usuario=get_usuario()
    edad=get_edad()
    password=get_password()
    print(f"Bienvenido {usuario}. Tu edad es {edad} y tu contraseña es {password}") """



